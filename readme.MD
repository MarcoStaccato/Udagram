# to create vpc stack run:

```./create.sh udagram-infra vpn/vpn.yml vpn/vpn.json```

# to create servers/LB associated with previous vpc run: 

```./create.sh udagram-servers servers/servers.yml servers/servers.json```